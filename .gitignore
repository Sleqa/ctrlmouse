# Build output (the exe ships via GitHub Releases, not the repo)
ctrlmouse.exe
*.obj
*.res
*.manifest
*.pdb
build/

# Per-user runtime settings
config.json

# Local tooling
.claude/

# Sibling projects in this folder - each is its own thing, not part of
# ctrlmouse. A checkpoint commit swept their build caches in here once.
GBAPal/
GBAPal-Remake/
ctrlmouse-astra/
gbamap/
